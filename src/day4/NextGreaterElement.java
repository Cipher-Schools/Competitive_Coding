package day4;


/**
 *
 * 1. Create a stack
 * 2. Start traversing from end in the array
 *  if s is empty  {
 *      for current element print -1 and push this current element in stack
 *  } else {
 *      if current element is greater than top of the stack {
 *          keep removing the elements from stack till it will become empty and stack is top is greater than current
 *      }
 *      print the top for this current element
 *      push the current element in stack
 *  }
 *
 */
public class NextGreaterElement {
}
